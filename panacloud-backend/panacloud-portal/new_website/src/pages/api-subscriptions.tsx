import React, { FC } from 'react';
import { Layout } from '../components';
import { PageProps } from 'gatsby';

const ApiSubscriptions: FC<PageProps> = ({ }) => {
    return (
        <Layout>
            Api Subscriptions Page
        </Layout>
    )
}

export default ApiSubscriptions;
